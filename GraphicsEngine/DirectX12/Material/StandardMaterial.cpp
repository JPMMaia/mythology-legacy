#include "pch.h"
#include "StandardMaterial.h"

using namespace GraphicsEngine;

/*std::shared_ptr<MaterialInstance> StandardMaterial::AddInstance(const DirectX::XMFLOAT4& baseColor)
{
	return m_materialsData.AddResource({baseColor});
}

void StandardMaterial::BindMaterialBuffer(ID3D12GraphicsCommandList* commandList)
{
	m_materialsData.BindBuffer(commandList);
}
*/