#include "CppUnitTest.h"

import Common.Timers;
import std.core;

using namespace Common;
//using namespace Microsoft::VisualStudio::CppUnitTestFramework;
using namespace std;

namespace GameEngineTests
{		
	/*TEST_CLASS(TimerTests)
	{
	public:
		
		//	After a call to the Reset and UpdateAndRender methods, the total and delta time should be equal.
		TEST_METHOD(Reset)
		{
			//std::cout << "Hello";
/*			Timer timer;
			
			timer.Reset();

			auto update = [](const Timer& timer)
			{
			};

			//std::chrono::time_point<std::chrono::steady_clock> y = t - x;

			//timer.UpdateAndRender(update, update, update, update, update);
			//Assert::AreEqual(timer.)

			timer.Reset();
			//timer.UpdateAndRender(update, update, update, update, update);*/
		/*}

		TEST_METHOD(FixedUpdate)
		{
			//Common::Timer timer;
		}

	};*/
}